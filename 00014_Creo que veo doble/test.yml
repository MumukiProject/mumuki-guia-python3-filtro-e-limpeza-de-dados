- name: 'Prueba misteriosa'
  keys:
    subset:
      contain: 
        expected: "lat"
        error: 'No pasó una de las pruebas misteriosas'
- name: 'Prueba misteriosa'
  keys:
    subset:
      contain: 
        expected: "long"
        error: 'No pasó una de las pruebas misteriosas'
- name: 'Prueba misteriosa'
  keys:
    subset:
      contain: 
        expected: "height"
        error: 'No pasó una de las pruebas misteriosas'
- name: 'Prueba misteriosa'
  keys:
    subset:
      contain: 
        expected: "diameter"
        error: 'No pasó una de las pruebas misteriosas'
- name: 'Prueba misteriosa'
  keys:
    subset:
      contain: 
        expected: "inclination"
        error: 'No pasó una de las pruebas misteriosas'
- name: 'Prueba misteriosa'
  keys:
    subset:
      match: 
        expected: '^((?!tree_id).)*$'
        error: 'No pasó una de las pruebas misteriosas'   
- name: 'Prueba misteriosa'
  keys:
    subset:
      match: 
        expected: '^((?!fol_type).)*$'
        error: 'No pasó una de las pruebas misteriosas'   
- name: 'Prueba misteriosa'
  keys:
    subset:
      match: 
        expected: '^((?!fam_name).)*$'
        error: 'No pasó una de las pruebas misteriosas'   
- name: 'Prueba misteriosa'
  keys:
    subset:
      match: 
        expected: 'gen_name|sci_name|comm_name'
        error: 'No pasó una de las pruebas misteriosas'   
                                
                                

        